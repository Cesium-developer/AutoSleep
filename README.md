# AutoSleep
Windows 守护程序，根据 CPU/GPU/网络负载、时间段及进程白名单，在满足条件时自动触发电脑睡眠。 / A Windows daemon that automatically triggers system sleep based on CPU/GPU/network thresholds, time windows, and process whitelist.
